---
import Link from '$/components/Link.astro';
import PersonaGroup from '@/components/Profile/PersonaGroup.astro';
import {
  DocumentPdfFilled,
  VideoClipFilled,
  VideoFilled,
} from '$/components/Icons/fluentui';
import MeetingCountdown from '@/components/Meeting/Countdown.astro';
import MeetingTypeBadge from '@/components/Meeting/TypeBadge';
import MeetingDifficultyBadge from '@/components/Meeting/DifficultyBadge';
import { convertDate, weekNumber } from '@/utils/meetings';
import { getProfilesFromNames } from '$/utils/profiles';
import type { MeetingIndexEntry } from '@/utils/meetings';

import dayjs from 'dayjs';
import duration from 'dayjs/plugin/duration';
import utc from 'dayjs/plugin/utc';

dayjs.extend(duration);
dayjs.extend(utc);

interface Props {
  input: MeetingIndexEntry;
  dateFormat?: string;
};

const { input, dateFormat } = Astro.props;
const entry = input.data;
const meeting = input.collection === 'meetings' ? input.data : undefined;
const slidesUrl = meeting?.slides ?? meeting?.slides_url;
const isEvent = input.collection === 'events';
const time_close = dayjs(entry.time_start).add(dayjs.duration(entry.duration)).toDate();
const isMaybeActive = dayjs().isBefore(time_close);

// Resolve credit names to profiles
const profiles = await getProfilesFromNames(entry.credit);
---
<style is:global>
  [data-is-active="true"] .meeting-live-video-button {
    display: flex;
  }
  [data-is-active="true"] .meeting-video-unavailable-button {
    display: none;
  }
</style>

<li
  data-maybe-active={isMaybeActive ? "true" : undefined}
  data-time-start={isMaybeActive ? entry.time_start.getTime() : undefined}
  data-time-close={isMaybeActive ? time_close.getTime() : undefined}
>
  <div class="group grid grid-cols-[1fr_max-content] px-2 -mx-2 gap-x-4 rounded-lg hover:bg-surface-200">
    <Link href={input.slug} class="!text-content py-1 m-0 flex flex-col lg:flex-row grow shrink min-w-0 gap-x-4 gap-y-1 lg:items-center">
      <div class="flex flex-col-reverse lg:flex-row grow-0 shrink-0 gap-x-4 lg:items-center">
        <time
          class:list={["font-mono", { "min-w-[20ch]": !dateFormat }]}
          datetime={convertDate(entry.time_start, "YYYY-MM-DDTHH:mm:ssZ", entry.timezone)}
          title={convertDate(entry.time_start, "dddd, MMMM Do, YYYY, h:mm A (zzz)", entry.timezone)}
          aria-label={convertDate(entry.time_start, "dddd, MMMM Do, YYYY, h:mm A (zzz)", entry.timezone)}
        >
          {dateFormat
            ? convertDate(entry.time_start, dateFormat, entry.timezone)
            : (
              convertDate(entry.time_start, "ddd MMM DD", entry.timezone) +
              ", " +
              (entry.time_start.getMinutes() == 0
                ? convertDate(entry.time_start, "h A", entry.timezone)
                : convertDate(entry.time_start, "h:mm A", entry.timezone)
              )
            )
          }
        </time>
        <MeetingTypeBadge type={isEvent ? 'event' : meeting?.type ?? 'general'} consistentWidth={true} />
      </div>
      <div class="lg:truncate w-full">
        <span class="align-middle">
          {meeting?.week_number != null ? (
            <span
              aria-hidden="true"
              title={`Week ${weekNumber(meeting.week_number)}`}
              class="px-1 font-mono rounded-lg border-2 border-surface-150 select-none">Wk {weekNumber(meeting.week_number)}</span>
          ) : null}
          {entry.title}
        </span>
        <span class="inline-block align-middle">
          {meeting?.difficulty ? (
            <MeetingDifficultyBadge difficulty={meeting.difficulty} />
          ) : null}
        </span>
      </div>
      <span class="w-fit flex flex-row grow-0 shrink-0 gap-x-2 text-base">
        {entry.location ? (
          <span class="opacity-90 italic">{entry.location}</span>
        ) : null}
        <MeetingCountdown time_start={entry.time_start} time_close={time_close} />
      </span>
    </Link>
    <div class="hidden sm:flex flex-row grow-0 shrink-0 gap-x-4 py-1 items-center min-w-fit">
      <div class="flex flex-row gap-x-2 items-center">
        {meeting?.recording ? (
          <Link
            href={meeting.recording}
            class="rounded-lg p-1 size-8 flex items-center justify-items-center bg-surface-150 border-2 border-surface-200 group-hover:bg-surface-200 group-hover:border-surface-150 hover:!border-primary"
            title="Watch video"
            aria-label="Watch video"
          >
            <VideoClipFilled />
          </Link>
        ) : isMaybeActive && meeting?.live_video_url ? (
          <span>
            <Link
              href={meeting.live_video_url}
              class="meeting-live-video-button hidden rounded-lg p-1 size-8 items-center justify-items-center !text-white bg-gradient-to-tr from-blue-800 to-blue-500 border-2 border-surface-200 group-hover:bg-surface-200 group-hover:border-surface-150 hover:!border-primary"
              title="Join live video"
              aria-label="Join live video"
            >
              <VideoFilled />
            </Link>
            <span
              class="meeting-video-unavailable-button flex cursor-not-allowed rounded-lg p-1 size-8 items-center justify-items-center border-2 border-surface-150 text-surface-150 group-hover:border-surface-250 group-hover:text-surface-250 border-dashed"
              title="Video unavailable"
            >
              <VideoClipFilled />
            </span>
          </span>
        ) : (
          <span
            class="cursor-not-allowed rounded-lg p-1 size-8 flex items-center justify-items-center border-2 border-surface-150 text-surface-150 group-hover:border-surface-250 group-hover:text-surface-250 border-dashed"
            title="Video unavailable"
          >
            <VideoClipFilled />
          </span>
        )}
        {slidesUrl ? (
          <Link
            href={slidesUrl}
            class="rounded-lg p-1 size-8 flex items-center justify-items-center bg-surface-150 border-2 border-surface-200 group-hover:bg-surface-200 group-hover:border-surface-150 hover:!border-primary"
            target="_blank" rel="noopener noreferrer"
            title={meeting?.slides ? "Download slides" : "View slides"}
            aria-label={meeting?.slides ? "Download slides" : "View slides"}
          >
            <DocumentPdfFilled />
          </Link>
        ) : (
          <span
            class="cursor-not-allowed rounded-lg p-1 size-8 flex items-center justify-items-center border-2 border-surface-150 text-surface-150 group-hover:border-surface-250 group-hover:text-surface-250 border-dashed"
            title="Slides unavailable"
          >
            <DocumentPdfFilled />
          </span>
        )}
      </div>
      <span class="hidden xl:block">
        <PersonaGroup
          profiles={profiles}
          limit={3}
        />
      </span>
    </div>
  </div>
  <hr class="border-surface-200" />
</li>
