---
import { getSemesterToMeetingIndexMap } from '@/utils/meetings';

const semesters = Array.from((await getSemesterToMeetingIndexMap()).keys());
const latestSemester = semesters[0];

return Astro.rewrite(`/meetings/${latestSemester.toLowerCase()}/`);
---
