---
import { getMeetings, getSemesters } from '@/utils/meetings';

const semesters = getSemesters((await getMeetings()));
const latestSemester = semesters[0];

return Astro.rewrite(`/meetings/${latestSemester.toLowerCase()}/`);
---