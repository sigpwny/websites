---
import { getFallCTFEvents } from '@/utils/fallctf-events';

const events = await getFallCTFEvents();
const latestSlug = events[0].slug;

// TODO: ensure that canonical URL points to year of latest event
return Astro.rewrite(latestSlug);
---